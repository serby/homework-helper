module.exports = createFileset

var src = __dirname + '/../../**/img/'

function createFileset (pliers) {
  pliers.filesets('images'
    , src + '**/*.{gif,jpg,png,svg}'
    // Ignore images generated by `pliers buildSprite`
    , src + 'sprite/generated/**/*'
  )
}
